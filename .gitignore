# macOS
.DS_Store
.AppleDouble
.LSOverride

# Backups
*.bak
*.orig

# Local secrets / env
.env
.env.*
*.local

# Per-machine, per-project design system config — generated by /fig-setup, never committed
design-system.config.js

# Claude Code local overrides — settings.local.json stays local, not committed
.claude/settings.local.json
.claude/projects/
.claude/todos/
.claude/memories/

# Editor
.vscode/
.idea/
